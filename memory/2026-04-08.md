Added a fifth bounded demo domain: `medical-workflow-router`.

Key decisions:
- Tightened the medical registry to remove accidental overlap between `routine_review` vs `lab_panel_priority_order` and `urgent_clinician_review` vs respiratory-pathway cases.
- Narrowed `escalate_supervisor_review` so it represents true ambiguous/high-risk escalation rather than swallowing generic `complex` urgent cases.
- Wired medical into shared tooling: `kvrm_tui.py`, `train_kvrm_model.py`, `kvrm_bench.demo`, `kvrm-demos/run_demo.py`, `kvrm-demos/compare_demos.py`, and `kvrm-demos/README.md`.

Architecture change:
- Fixed a learned-selector false-accept bug in `kvrm_core.learned.CompactLearnedSelector`.
- The learned branch now honors a domain-level unsupported-feature predicate before emitting candidates.
- Propagated that guard through SOC, SRE, drone, finance, and medical learned selectors, and through `train_kvrm_model.py`.

Current verified outcomes:
- Medical hybrid benchmark: semantic correctness `1.0`, false accept `0.0`, unsupported rejection `1.0`, OOD supported accuracy `1.0`.
- Medical compact training report after the guard fix: selector-only `0.7222 / 0.0000 / 1.0000`, hybrid-augmented `1.0000 / 0.0000 / 1.0000` for accuracy / false-accept / unsupported rejection.
- Medical interactive demo smoke passes with the trained model.
