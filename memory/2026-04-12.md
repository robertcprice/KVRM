# 2026-04-12

- Added generated publication paper assets under `kvrm-bench/results/publication_bundle/`: `paper_assets.json`, `paper_tables.md`, and `figure_captions.md`.
- Updated the publication bundle script to emit those paper assets after summary generation and added regression coverage in `tests/kvrm_bench/test_publication_paper_assets.py`.
- Synced `docs/papers/KVRM_FLAGSHIP_PAPER_DRAFT.md` to the live seven-domain artifact set:
  - Section 6 now refers to the canonical seven-domain suite.
  - The canonical-results table now includes IAM.
  - The support-gate stress table now includes IAM.
  - The draft now points at the generated publication summary, paper tables, and figure captions as canonical paper-facing artifacts.
- Tightened `docs/papers/KVRM_EVIDENCE_MATRIX.md` so the next-baseline recommendation matches the current six-domain external comparison subset.
- Added a generated publication appendix path:
  - bundle output at `kvrm-bench/results/publication_bundle/paper_appendix.md`
  - synced docs copy at `docs/papers/KVRM_PUBLICATION_APPENDIX.md`
- Extended `kvrm_bench.publication_paper_assets` with appendix rendering/build helpers so the paper workflow now has one refreshable manuscript snapshot instead of separate table/caption fragments only.
- Updated `kvrm-bench/scripts/run_publication_bundle.py` to write the appendix into both the bundle and `docs/papers/`, and added regression coverage for appendix generation in `tests/kvrm_bench/test_publication_paper_assets.py`.
- Reduced manuscript drift in `docs/papers/KVRM_FLAGSHIP_PAPER_DRAFT.md` by removing duplicated benchmark tables from Sections 6-8 and replacing them with references to the generated appendix:
  - Appendix Table 1 for canonical seven-domain results
  - Appendix Table 2 plus Figures 5-6 for architecture-native safety evidence
  - Appendix Table 3 plus Figure 7 for robustness-family totals
  - Appendix Table 5 for the external-baseline summary
  - Appendix Table 6 for the ambiguity frontier
- Added paper-doc organization guardrails:
  - `docs/papers/KVRM_FLAGSHIP_PAPER_SCAFFOLD.md` now tells future rewrites to treat `docs/papers/KVRM_PUBLICATION_APPENDIX.md` as the canonical manuscript-side source for tables/captions.
  - `docs/papers/KVRM_FIGURE_SOURCE_MAP.md` now points at the synced appendix as the manuscript-facing caption snapshot.
  - `tests/kvrm_bench/test_paper_docs_sync.py` now asserts the flagship draft uses appendix references and does not reintroduce duplicated benchmark tables.
- Cleaned the scaffold itself to match the appendix-backed workflow:
  - removed duplicated internal-baseline/support-gate/fallback/robustness tables from `docs/papers/KVRM_FLAGSHIP_PAPER_SCAFFOLD.md`
  - fixed the scaffold demo-domain list to include IAM and explicit 24-case counts for grid/finance/medical/IAM
  - added `docs/papers/README.md` as the paper-workflow entrypoint
- Wired the paper workflow entrypoint through the surrounding docs:
  - `kvrm-bench/README.md`
  - `kvrm-demos/README.md`
  - `docs/reports/KVRM_PUBLICATION_READINESS_2026-04-08.md`
  - `docs/papers/KVRM_FLAGSHIP_PAPER_DRAFT.md`
  - `docs/papers/KVRM_EVIDENCE_MATRIX.md`
- Added a generated paper-doc audit layer:
  - new module `kvrm_bench.paper_doc_audit`
  - new CLI `kvrm-bench/scripts/run_paper_doc_audit.py`
  - new tests in `tests/kvrm_bench/test_paper_doc_audit.py`
  - `run_publication_bundle.py` now emits `paper_doc_audit.json` and `paper_doc_audit.md`
  - live audit currently reports `all_checks_passed=true` with 11/11 checks passing
- Added a generated publication-portal layer for the bundle root:
  - new module `kvrm_bench.publication_portal`
  - new test `tests/kvrm_bench/test_publication_portal.py`
  - `run_publication_bundle.py` now emits `publication_portal.json` and `publication_portal.md`
  - the portal surfaces artifact counts, canonical-suite counts, paper-doc audit status, headline claims, and quick links to the manifest/summary/appendix/audit/docs
- Refined the publication portal so it also writes `kvrm-bench/results/publication_bundle/README.md`, making the bundle root open directly on the generated landing page instead of requiring a second click into `publication_portal.md`.
- Added a one-command publication readiness layer:
  - new module `kvrm_bench.publication_check`
  - new CLI `kvrm-bench/scripts/run_publication_check.py`
  - new tests in `tests/kvrm_bench/test_publication_check.py`
  - the readiness pass refreshes the publication bundle, reruns the paper-doc audit, runs `pytest tests/kvrm_bench -q`, writes `publication_check.json` and `publication_check.md`, and refreshes the publication portal with the latest check status
- Tightened the paper/workflow docs so the generated artifact lists now explicitly include the readiness artifacts and the bundle landing-page files:
  - `kvrm-bench/README.md`
  - `docs/papers/README.md`
  - `tests/kvrm_bench/test_paper_docs_sync.py` now guards those references
- Converted the flagship paper draft to citekey-backed literature references tied directly to `docs/papers/kvrm_refs.bib` and removed the duplicate hand-maintained references block at the end of the draft.
- Extended `kvrm_bench.paper_doc_audit` with citation/bibliography checks:
  - draft citekeys present
  - draft citekeys resolve in `docs/papers/kvrm_refs.bib`
  - duplicate draft references block absent
  - live paper-doc audit is now `14/14`
- Added a dedicated manuscript handoff packet:
  - new module `kvrm_bench.publication_manuscript_packet`
  - new test `tests/kvrm_bench/test_publication_manuscript_packet.py`
  - `run_publication_bundle.py` now emits `kvrm-bench/results/publication_bundle/manuscript/{README.md,manifest.json,manifest.md}` plus copied manuscript-facing files
  - the publication portal now links the manuscript packet directly from the bundle root
- Tightened the flagship manuscript tone and added a guardrail against regressions:
  - `docs/papers/KVRM_FLAGSHIP_PAPER_DRAFT.md` now uses `Manuscript Status` language instead of internal-revision markers
  - the draft now says `The manuscript advances five core claims` / `Primary artifacts for this manuscript`
  - `kvrm_bench.paper_doc_audit` now checks `draft internal-only markers absent`
  - live paper-doc audit is now `15/15`
- Added deterministic venue-export automation:
  - new module `kvrm_bench.publication_submission_export`
  - new CLI `kvrm-bench/scripts/run_publication_submission_export.py`
  - `run_publication_bundle.py` now emits `kvrm-bench/results/publication_bundle/submission/`
  - the submission export copies the manuscript packet inputs and renders both the flagship manuscript and appendix to `.tex` and `.pdf` via `pandoc` + `pdflatex`
  - the publication portal now links `submission/README.md` directly from the bundle root
  - live submission export currently reports `all_exports_succeeded=true`, `pandoc 3.9`, and `pdfTeX 3.141592653-2.6-1.40.27`
- Increased the publication-check stdout tail budget so `publication_check.md` no longer truncates the bundle output list now that manuscript + submission artifacts are included.
- Tightened the submission export so it is closer to an actual review packet:
  - exported sources are now normalized to `paper.md`, `appendix.md`, and `references.bib` instead of working-manuscript filenames
  - the submission paper source strips repo-only sections such as `Manuscript Status`, `Artifact References`, and `Bibliography Workflow`
  - the submission manifest now records PDF metadata and checks `all_pdf_authors_blank=true`
  - the submission export now also writes `submission_bundle.zip` for handoff
- Latest readiness run:
  - `all_checks_passed=true`
  - `passed_check_count=3`
  - `paper_doc_audit=15/15`
  - `tests/kvrm_bench=74 passed`
- Extended the submission export into a bundle-level profile matrix so publication artifacts stop overwriting each other:
  - `kvrm_bench.publication_submission_export` now supports `dirname=...` plus `build_publication_submission_export_matrix(...)`
  - the bundle now writes a root submission index at `kvrm-bench/results/publication_bundle/submission/README.md`
  - review and working-manuscript packets now live side by side under `submission/review-anonymous/` and `submission/working-manuscript/`
  - the anonymous profile still strips repo-only workflow sections and now feeds Pandoc title-aware markdown with YAML frontmatter plus heading shifting
- `run_publication_bundle.py` now emits the submission matrix by default, while `run_publication_submission_export.py --all-profiles` mirrors that same side-by-side layout on demand.
- Updated publication-facing docs/tests to match the cleaner submission layout:
  - `kvrm-bench/README.md`
  - `docs/papers/README.md`
  - `tests/kvrm_bench/test_publication_submission_export.py`
  - `tests/kvrm_bench/test_publication_portal.py`
  - `tests/kvrm_bench/test_paper_docs_sync.py`
- Fixed the shared test bootstrap so plain repo-root pytest runs no longer depend on external `PYTHONPATH` injection:
  - `tests/conftest.py` now adds grid, finance, medical, and IAM demo src paths in addition to the original domains
- Latest readiness run after the submission-matrix cleanup:
  - `all_checks_passed=true`
  - `passed_check_count=3`
  - `paper_doc_audit=15/15`
  - `tests/kvrm_bench=75 passed`
