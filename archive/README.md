# archive/

Superseded and historical material, retained for provenance but **not part of
the current flagship artifact**. Nothing here is imported by `kvrm-core`,
`kvrm-bench`, or the publication pipeline.

- `white-paper/` — earlier KVRM write-up, superseded by `docs/papers/`.
- `memory/` — older session/working notes.
- `train_all_100_percent.py` — historical bulk-training script (the current
  entry point is `scripts/train_kvrm_model.py`).
- `logos-kvrm-files/`, `pytorch-build/` — earlier experimental material.

Safe to ignore when reading the codebase; safe to prune if disk space is needed.
