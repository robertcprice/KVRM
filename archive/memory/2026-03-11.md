Worked in `kvrm-gpu` to narrow the active repo scope to KVRM selection and remove KV-cache/compression from current public positioning docs.

Added a real `benchmarks/kvrm_vs_xgrammar_benchmark.py` harness for two comparable tasks:
- finite enum emission
- tool-call JSON with a finite action registry

Key local result on Apple MPS with `Qwen/Qwen2.5-0.5B-Instruct` and the default non-quick config:
- finite enum: KVRM `100%` valid / `98.4%` correct / `0.158ms` p50 vs XGrammar `100%` valid / `67.2%` correct / `93.4ms` p50
- tool-call JSON: KVRM `100%` valid / `98.4%` correct / `0.326ms` p50 vs XGrammar `100%` valid / `70.3%` correct / `575.9ms` p50

Important implementation lesson:
- XGrammar JSON benchmarking needed compact schema compilation (`any_whitespace=False`, compact separators) because weak models can burn token budget on legal whitespace and make the comparison meaningless.

Repo verification state after changes:
- `./venv/bin/python -m pytest tests -q` -> `14 passed`

Further work in `kvrm-gpu`:
- added `benchmarks/registry_drift_benchmark.py` and ran it locally on Apple MPS
- drift result: unchecked registry reorder preserved `100%` structural validity but dropped semantic correctness to `4.7%`; stable-key adapter restored `95.3%`; strict digest guards fail closed
- ran `benchmarks/uncertainty_rejection_benchmark.py`: baseline `83.8%`, entropy-error correlation `0.683`, best point about `18.5%` rejection for `+11.3%` kept-set accuracy
- extended the uncertainty benchmark with calibration metrics: ECE(10) `0.070`, MC ECE(10) `0.040`, Brier `0.247`, AURC `0.046`
- added reusable registry-contract utilities and tests:
  - `gpu/control_plane/registry_contract.py`
  - `tests/test_registry_contract.py`
- added `benchmarks/registry_contract_audit.py` and confirmed `5/5` active control-plane registries are valid with stable digests
- reran deep verification phases:
  - phase 4 fuzzing: `5,456` inputs, `0` invalid, `0` crashes
  - phase 5 deployment verification: `2,508` inputs, `0` invalid, `19.10%` cross-backend selection mismatches
  - phase 6 certified robustness fixed for Python 3.14 and passed: `100%` certified at `epsilon=0.001`, `99%` at `0.005`, `96%` at `0.010`
- marked high-visibility KV/vLLM-era docs and paper exports as archival with explicit banners
- added `docs/LEGACY_MATERIALS.md` to separate active evidence from archive material
- test suite after adding registry-contract tests: `21 passed`

Additional work later the same day:
- Added `kvrm-gpu/benchmarks/registry_drift_benchmark.py` to prove runtime registry drift behavior explicitly.
- Quick local registry drift result:
  - baseline v1: `100%` valid / `100%` correct
  - reordered runtime without versioning: `100%` valid / `0%` correct
  - reordered runtime with key versioning: `100%` valid / `100%` correct
  - expanded runtime without retraining: `100%` valid / `64.8%` correct / `0%` recall on new key
- This matters because it proves KVRM validity can survive registry drift while semantics fail; versioning is required.

- Ran more robustness evidence:
  - `benchmarks/uncertainty_rejection_benchmark.py`: baseline `83.7%`, entropy-error corr `0.692`, best accepted-set improvement `+11.6%` at `19.0%` rejection
  - `benchmarks/registry_entropy_benchmark.py`: average normalized entropy `77.8%`, coverage `100%`, dead entries `0`
  - `benchmarks/kvrm_hierbench.py --train-samples 1000 --test-samples 300 --epochs 30`: `0%` invalid across in-distribution, OOD, and noisy-input experiments
  - `benchmarks/large_scale_registry_benchmark.py --quick`: hierarchical KVRM reached `100%` accuracy with `0` invalid at both 1k and 10k tested structures

- Fixed `papers/kvrm-selection/verification/certified_robustness.py` for Python 3.14 by removing the broken `math.erfinv` fallback.
- After the fix, phase 6 completed successfully with:
  - `100%` certified robustness at `epsilon=0.001`
  - CROWN / alpha-CROWN `99.0%` at `epsilon=0.005`
  - CROWN / alpha-CROWN `96.0%` at `epsilon=0.010`

- Marked high-visibility legacy KV-cache / vLLM docs and companion papers as archive or legacy so they stop conflicting with the active selection-only positioning.
- Updated `docs/ROBUSTNESS_EVIDENCE_MATRIX.md` and `papers/kvrm-selection/README.md` to reflect actual current evidence rather than old scope.

Additional robustness work:
- Added `benchmarks/registry_drift_benchmark.py` and ran quick mode.
- Quick drift result on CPU:
  - baseline `95.3%` semantic correctness
  - reordered unversioned registry: `100%` structural validity but only `4.7%` semantic correctness
  - stable-key adapter restored `95.3%`
  - strict digest guard produced `100%` safe rejection on incompatible registries
- Ran `benchmarks/uncertainty_rejection_benchmark.py`:
  - baseline accuracy `83.7%`
  - entropy-error correlation `0.692`
  - best operating point: about `19.0%` rejection for `+11.6%` kept-set accuracy improvement
- Added `docs/ROBUSTNESS_EVIDENCE_MATRIX.md` and `docs/LEGACY_MATERIALS.md`
- Updated active docs to include robustness commands/results and marked older KV/vLLM materials as legacy context

Deep verification note:
- `papers/kvrm-selection/verification/run_all.py --phases 4 5 6` produced:
  - Phase 4 PASS: `5,456` inputs, `0` invalid, `0` crashes
  - Phase 5 PASS: `1,504` inputs, `0` invalid, but `31.85%` backend selection mismatches
  - Phase 6 initially failed in one combined run during randomized smoothing, but a standalone rerun passed
- Standalone Phase 6 result:
  - CROWN / alpha-CROWN certified-trained model: `100%` certified at `ε=0.001`, `99%` at `ε=0.005`, `96%` at `ε=0.010`, `84.5%` at `ε=0.050`
  - randomized smoothing: `100/100` certified
  - PGD empirical robustness: `100%` at `ε=0.001` and `ε=0.010`, `90%` at `ε=0.050`, `80%` at `ε=0.100`

Adaptive tile benchmark correction:
- Found a benchmark validity bug in `gpu/optimizations/adaptive_flash_attention.py`: `force_tile_size` did not bypass the native SDPA fast path, so the prior "adaptive" benchmark was not actually timing tiled adaptive execution.
- Fixed `AdaptiveFlashAttention.forward(...)` to skip the native fast path when `force_tile_size` is provided or `allow_native_impl=False`, and to support `allow_native_kernel` at init-time for qualification runs.
- Added `tests/test_adaptive_flash_attention.py` to lock the bug fix in.
- Current benchmark file now acts as an adaptive-tile qualification harness:
  - sweeps all valid tile sizes on the real tiled path
  - finds the measured oracle tile
  - compares the cold selector and profiled selector to that oracle
  - checks numerical agreement against PyTorch SDPA
- Current local CPU benchmark result (`benchmarks/adaptive_tile_benchmark.py`, default `warmup=3`, `iterations=12`):
  - cold selector oracle match rate: `1/5`
  - profiled selector oracle match rate: `5/5`
  - cold mean regret: `27.58%`
  - profiled mean speedup vs fixed `64`: `1.35x`
  - max abs diff vs reference attention: `8.34e-07`
- Updated active docs to retire the old `1.58x` / `2.68x` adaptive-tile headline and describe the corrected qualification result instead.
- Full test suite after the new regression tests: `24 passed`

Final adaptive-tile artifact correction later the same session:
- Closed background subagents and reran `benchmarks/adaptive_tile_benchmark.py --iterations 8` directly on Apple MPS to avoid mixed CPU/MPS snapshots in docs/results.
- Final active artifact is now `kvrm-gpu/benchmarks/adaptive_tile_benchmark_results.json` with:
  - cold selector oracle match rate: `1/5`
  - profiled selector oracle match rate: `5/5`
  - cold mean regret: `68.87%`
  - profiled mean speedup vs fixed `64`: `3.80x`
  - max abs diff vs reference attention: `4.77e-07`
- Updated active docs (`README.md`, `EXPERIMENTS.md`, `docs/CLAIM_QUALIFICATION_PLAN.md`, `docs/RESEARCH_VALIDATION_AUDIT_2026-03-11.md`, `docs/ROBUSTNESS_EVIDENCE_MATRIX.md`, `papers/kvrm-selection/README.md`) to reflect the final MPS artifact.
- Final repo claim after this correction: cold adaptive tile heuristics are not yet robust; the defensible result is numerical correctness plus profiled-selector convergence on seen workloads.

Real-activation calibration follow-up:
- Added profile persistence to `gpu/optimizations/adaptive_flash_attention.py`:
  - `backend_name()`
  - `export_profile()`
  - `load_profile()`
  - `save_profile()`
  - `load_profile_from_file()`
- Added profile roundtrip regression coverage in `tests/test_adaptive_flash_attention.py`.
- Added `benchmarks/real_attention_tile_calibration.py` to derive tile oracles from real `distilgpt2` attention activations on real Wikitext-2 text.
- Final active artifact after reruns:
  - `kvrm-gpu/benchmarks/real_attention_tile_calibration_results.json`
  - `kvrm-gpu/benchmarks/real_attention_tile_profile.json`
- Final Apple MPS calibration result:
  - held-out attention records: `18`
  - cold heuristic oracle match rate: `0.0%`
  - calibrated profile oracle match rate: `66.7%`
  - cold mean regret: `76.44%`
  - calibrated mean regret: `2.51%`
  - calibrated mean speedup vs fixed `64`: `1.43x`
  - saved profile mapping: `128 -> 128`, `256 -> 256`, `512 -> 512`
- Updated active docs (`README.md`, `EXPERIMENTS.md`, `benchmarks/README.md`, `docs/CLAIM_QUALIFICATION_PLAN.md`, `docs/RESEARCH_VALIDATION_AUDIT_2026-03-11.md`, `docs/ROBUSTNESS_EVIDENCE_MATRIX.md`, `papers/kvrm-selection/README.md`) to use the real-activation calibration as the strongest adaptive-tile evidence.
- Test suite after new profile persistence test: `25 passed`

Measured-oracle XGrammar follow-up:
- Extended `benchmarks/kvrm_vs_xgrammar_benchmark.py` with an in-domain calibrated selector baseline built from the benchmark's own measured train workloads.
- New artifact: `kvrm-gpu/benchmarks/kvrm_vs_xgrammar_results_profiled_in_domain_quick.json`
- Quick Apple MPS result with `HuggingFaceTB/SmolLM2-135M-Instruct`:
  - finite enum: KVRM `50%`, heuristic `0%`, saved runtime profile `25%`, in-domain profile `50%`, XGrammar `0%`
  - tool-call JSON: KVRM `50%`, heuristic `0%`, saved runtime profile `25%`, in-domain profile `50%`, XGrammar `0%`
  - saved `distilgpt2` profile only hit `50%` of sampled seq lengths; in-domain profile hit `100%`
- Main conclusion: the previous transfer failure was partly a domain-mismatch story, not proof that profile-based selection is useless. The stronger non-neural baseline against KVRM is now an in-domain calibrated selector, not the cold heuristic.

Measured-oracle multi-seed follow-up:
- Added `kvrm-gpu/benchmarks/kvrm_vs_xgrammar_multiseed.py`
- Added `kvrm-gpu/tests/test_kvrm_vs_xgrammar_multiseed.py`
- New artifact: `kvrm-gpu/benchmarks/kvrm_vs_xgrammar_multiseed_results_mps_quick.json`
- Apple MPS quick seeds `42-44` with `HuggingFaceTB/SmolLM2-135M-Instruct`:
  - KVRM mean correctness: `83.3%`
  - saved runtime profile mean correctness: `50.0%`
  - in-domain calibrated profile mean correctness: `41.7%`
  - XGrammar mean correctness: `0%`
  - in-domain profile mean hit rate: `83.3%`
- Important nuance: quick in-domain coverage varies by seed because some train splits do not include every sequence length. `profile_hit_rate` is therefore a real benchmark-quality metric, not just metadata.
- Additional nuance after fair-timing rerun:
  - KVRM latency was updated to use explicit device synchronization, matching the XGrammar timing path
  - pooled XGrammar JSON validity on the 3-seed quick run was `91.7%` (`11/12`)
  - the simple seq-length-only in-domain calibration is less stable than the earlier first pass suggested

Richer non-neural baseline follow-up:
- Renamed the measured-oracle benchmark baselines for clarity:
  - `saved_runtime_profile`
  - `in_domain_seq_len_profile`
  - `in_domain_retrieval_profile`
- Added a measured train-workload retrieval baseline to `kvrm-gpu/benchmarks/kvrm_vs_xgrammar_benchmark.py`
- Current quick Apple MPS 3-seed artifact:
  - KVRM pooled correctness: `50.0%`
  - saved runtime profile pooled correctness: `50.0%`
  - in-domain seq-length profile pooled correctness: `58.3%`
  - in-domain retrieval profile pooled correctness: `75.0%`
  - XGrammar pooled correctness: `0%`
- Main conclusion: the stronger non-neural comparison point is now the retrieval baseline, and the quick MPS benchmark is still too timing-sensitive to use as a clean headline result.

Higher-stability MPS multi-seed follow-up:
- Validated the latest benchmark patch with:
  - `./venv/bin/python -m py_compile benchmarks/kvrm_vs_xgrammar_benchmark.py benchmarks/kvrm_vs_xgrammar_multiseed.py`
  - `./venv/bin/python -m pytest tests -q` -> `29 passed`
- Ran a stronger real MPS sweep:
  - `./venv/bin/python benchmarks/kvrm_vs_xgrammar_multiseed.py --quick --device mps --model HuggingFaceTB/SmolLM2-135M-Instruct --num-seeds 5 --oracle-warmup 2 --oracle-iterations 4 --output benchmarks/kvrm_vs_xgrammar_multiseed_results_mps_stable5.json`
- New active artifact:
  - `kvrm-gpu/benchmarks/kvrm_vs_xgrammar_multiseed_results_mps_stable5.json`
- Apple MPS quick seeds `42-46`, pooled over `20` real samples per task:
  - finite enum and tool-call JSON both showed:
    - KVRM: `70.0%` (`14/20`, Wilson 95% `[48.1, 85.5]`)
    - saved runtime profile: `40.0%` (`8/20`)
    - in-domain seq-length profile: `65.0%` (`13/20`)
    - in-domain retrieval profile: `75.0%` (`15/20`)
    - XGrammar: `0%` semantic correctness
  - tool-call JSON XGrammar structural validity pooled at `90.0%` (`18/20`)
  - mean p50 latency:
    - KVRM `0.380ms` enum / `0.384ms` JSON
    - retrieval baseline `0.013ms` enum / `0.016ms` JSON
    - XGrammar `92.3ms` enum / `1052.5ms` JSON
  - mean profile hit rate:
    - saved runtime profile `45.0%`
    - in-domain seq-length profile `75.0%`
- Main conclusion after the stronger sweep:
  - the retrieval baseline is still the strongest non-neural comparator
  - KVRM improved materially over the older 3-seed quick artifact and is now close to retrieval, but still does not beat it
  - XGrammar still trails badly on semantics with this small model and is not perfectly valid on the JSON task
- Updated active docs to promote the stable 5-seed artifact and stop repeating the old `50.0% / 58.3% / 75.0% / 91.7%` numbers:
  - `kvrm-gpu/README.md`
  - `kvrm-gpu/EXPERIMENTS.md`
  - `kvrm-gpu/benchmarks/README.md`
  - `kvrm-gpu/docs/KVRM_VS_XGRAMMAR.md`
  - `kvrm-gpu/docs/CLAIM_QUALIFICATION_PLAN.md`
  - `kvrm-gpu/docs/KVRM_XGRAMMAR_BENCHMARK_METHOD.md`

Non-quick multi-seed backend qualification:
- Confirmed local backend availability:
  - `mps=True`
  - `cuda=False`
  - `cuda_device_count=0`
- Ran the first larger real-workload multi-seed MPS artifact:
  - `./venv/bin/python benchmarks/kvrm_vs_xgrammar_multiseed.py --device mps --model HuggingFaceTB/SmolLM2-135M-Instruct --num-seeds 5 --output benchmarks/kvrm_vs_xgrammar_multiseed_results_mps_nonquick5.json`
- Active local reference artifact is now:
  - `kvrm-gpu/benchmarks/kvrm_vs_xgrammar_multiseed_results_mps_nonquick5.json`
- Non-quick Apple MPS pooled over `40` real samples per task:
  - KVRM: `77.5%` (`31/40`, Wilson 95% `[62.5, 87.7]`)
  - saved runtime profile: `57.5%` (`23/40`)
  - in-domain seq-length profile: `92.5%` (`37/40`)
  - in-domain retrieval profile: `80.0%` (`32/40`)
  - XGrammar: `0/40` semantic correctness, JSON validity `36/40` = `90.0%`
  - mean p50 latency:
    - KVRM `0.370ms` enum / `0.398ms` JSON
    - seq-length profile `0.000ms` enum / `0.002ms` JSON
    - retrieval `0.020ms` enum / `0.023ms` JSON
    - XGrammar `88.6ms` enum / `988.9ms` JSON
- Important interpretation:
  - the non-quick MPS split gives the seq-length profile `100%` profile coverage, which changes the ranking and makes it the strongest non-neural baseline on MPS
  - KVRM improves materially over the quick artifacts but still does not beat the best non-neural comparator

- Ran a matching non-quick CPU backend check since no CUDA hardware is available:
  - `./venv/bin/python benchmarks/kvrm_vs_xgrammar_multiseed.py --device cpu --model HuggingFaceTB/SmolLM2-135M-Instruct --num-seeds 5 --output benchmarks/kvrm_vs_xgrammar_multiseed_results_cpu_nonquick5.json`
- CPU pooled over `40` real samples per task:
  - KVRM: `65.0%` (`26/40`, Wilson 95% `[49.5, 77.9]`)
  - saved runtime profile: `52.5%` (`21/40`)
  - in-domain seq-length profile: `57.5%` (`23/40`)
  - in-domain retrieval profile: `72.5%` (`29/40`)
  - XGrammar finite-enum: `1/40` = `2.5%`; tool-call JSON: `0/40`; JSON validity `36/40` = `90.0%`
- Important interpretation:
  - the best non-neural comparator is backend-sensitive: MPS favors the seq-length profile, CPU favors retrieval
  - KVRM is competitive on both backends but still not the top selector in the active non-quick artifacts

- Updated active docs to promote the non-quick MPS artifact and add the CPU backend comparison:
  - `kvrm-gpu/README.md`
  - `kvrm-gpu/EXPERIMENTS.md`
  - `kvrm-gpu/benchmarks/README.md`
  - `kvrm-gpu/docs/KVRM_VS_XGRAMMAR.md`
  - `kvrm-gpu/docs/CLAIM_QUALIFICATION_PLAN.md`
  - `kvrm-gpu/docs/KVRM_XGRAMMAR_BENCHMARK_METHOD.md`
- Reran tests after the doc update:
  - `./venv/bin/python -m pytest tests -q` -> `29 passed`

Stronger-model follow-up after the user asked for "both":
- Reconfirmed local hardware limit:
  - still `mps=True`, `cuda=False`
  - no local CUDA artifact can be produced on this machine without external hardware
- Used a stronger cached local model on the same real harness:
  - `./venv/bin/python benchmarks/kvrm_vs_xgrammar_multiseed.py --device mps --model Qwen/Qwen2.5-0.5B-Instruct --num-seeds 5 --output benchmarks/kvrm_vs_xgrammar_multiseed_results_mps_nonquick5_qwen_0_5b.json`
- New stronger-model MPS artifact:
  - `kvrm-gpu/benchmarks/kvrm_vs_xgrammar_multiseed_results_mps_nonquick5_qwen_0_5b.json`
- Qwen non-quick Apple MPS pooled over `40` real samples per task:
  - KVRM: `90.0%` (`36/40`, Wilson 95% `[76.9, 96.0]`)
  - saved runtime profile: `67.5%` (`27/40`)
  - in-domain seq-length profile: `82.5%` (`33/40`)
  - in-domain retrieval profile: `85.0%` (`34/40`)
  - XGrammar: `0/40` semantic correctness
  - XGrammar JSON validity: `40/40` = `100.0%`
  - mean p50 latency:
    - KVRM `0.337ms` enum / `0.353ms` JSON
    - retrieval `0.019ms` enum / `0.020ms` JSON
    - XGrammar `109.2ms` enum / `758.9ms` JSON
- Important interpretation:
  - stronger model quality changes the ranking again: KVRM becomes the best selector on MPS
  - XGrammar benefits structurally from the stronger model and reaches perfect JSON validity, but semantic correctness still does not move
  - the honest repo position is now that performance is both backend-sensitive and model-sensitive

- Updated docs to add the Qwen corroboration while keeping the SmolLM MPS/CPU non-quick artifacts as the main cross-backend reference set:
  - `kvrm-gpu/README.md`
  - `kvrm-gpu/EXPERIMENTS.md`
  - `kvrm-gpu/benchmarks/README.md`
  - `kvrm-gpu/docs/KVRM_VS_XGRAMMAR.md`
  - `kvrm-gpu/docs/CLAIM_QUALIFICATION_PLAN.md`
- Reran tests again after the doc update:
  - `./venv/bin/python -m pytest tests -q` -> `29 passed`

Vast.ai / CUDA follow-up after the user said "use vastai command":
- Confirmed local `vastai` CLI works and is authenticated.
- Used `vastai show instances` and found two running H200 instances:
  - `32666214` on `ssh3.vast.ai`
  - `32678361` on `ssh7.vast.ai`
- Used `vastai ssh-url` to resolve connection details and then SSH'd in to inspect the remote environment.
- The chosen H200 box had:
  - CUDA available (`NVIDIA H200`, driver `580.95.05`)
  - Python `3.11.10`
  - no repo checkout in `/workspace`
  - no warm Hugging Face cache for the benchmark models
- Synced the current local repo subtree to the instance:
  - `/Users/bobbyprice/projects/KVRM/kvrm-gpu` -> `/workspace/kvrm-gpu`
- Created remote venv with system CUDA torch:
  - `python3 -m venv --system-site-packages /workspace/kvrm-gpu/venv`
  - installed `transformers==4.57.6`, `xgrammar==0.1.32`, `sentencepiece`
- First CUDA benchmark launch failed because:
  - `PYTHONPATH` was missing, so `gpu` could not be imported
- Second launch failed because:
  - Triton / XGrammar on CUDA needed a C compiler and the base container had none
- Fixed the remote infra blocker with:
  - `apt-get update && apt-get install -y build-essential`
- Warmed the remote HF cache with:
  - `snapshot_download("HuggingFaceTB/SmolLM2-135M-Instruct")`
- Successful real CUDA run:
  - remote command:
    - `PYTHONPATH=/workspace/kvrm-gpu ./venv/bin/python benchmarks/kvrm_vs_xgrammar_multiseed.py --device cuda --model HuggingFaceTB/SmolLM2-135M-Instruct --num-seeds 5 --output benchmarks/kvrm_vs_xgrammar_multiseed_results_cuda_nonquick5_h200.json`
  - synced artifact back to local:
    - `kvrm-gpu/benchmarks/kvrm_vs_xgrammar_multiseed_results_cuda_nonquick5_h200.json`
- CUDA H200 pooled over `40` real samples per task:
  - KVRM: `65.0%` (`26/40`, Wilson 95% `[49.5, 77.9]`)
  - saved runtime profile: `20.0%` (`8/40`) with `0%` profile loaded / hit
  - in-domain seq-length profile: `60.0%` (`24/40`)
  - in-domain retrieval profile: `67.5%` (`27/40`)
  - XGrammar: `0/40` semantic correctness, JSON validity `36/40` = `90.0%`
  - mean p50 latency:
    - KVRM `0.114ms` enum / `0.120ms` JSON
    - retrieval `0.037ms` enum / `0.041ms` JSON
    - XGrammar `102.0ms` enum / `873.8ms` JSON
- Important interpretation:
  - first actual CUDA artifact is now complete
  - weaker-model CUDA behaves more like CPU than MPS: retrieval remains best, KVRM is close behind
  - the saved runtime profile result on CUDA is not a fair selector comparison yet because there was no backend-compatible saved CUDA profile to load

- Updated docs again to include the real Vast.ai CUDA artifact:
  - `kvrm-gpu/README.md`
  - `kvrm-gpu/EXPERIMENTS.md`
  - `kvrm-gpu/benchmarks/README.md`
  - `kvrm-gpu/docs/KVRM_VS_XGRAMMAR.md`
  - `kvrm-gpu/docs/CLAIM_QUALIFICATION_PLAN.md`

XGrammar failure-mode diagnosis later the same day:
- patched `kvrm-gpu/benchmarks/kvrm_vs_xgrammar_benchmark.py` to emit:
  - `oracle_tile_histogram`
  - `raw_tile_histogram`
  - `decoded_tile_histogram`
  - `mode_prediction`
  - `oracle_frequency_of_mode_prediction`
- added XGrammar diagnostic controls:
  - `--xgrammar-num-shots`
  - `--xgrammar-label-permutation`
  - `--xgrammar-permutation-seed`
- updated `kvrm-gpu/benchmarks/kvrm_vs_xgrammar_multiseed.py` to pass those flags through
- added unit coverage in `kvrm-gpu/tests/test_kvrm_vs_xgrammar_benchmark_helpers.py`
- repo tests after the patch: `33 passed`

Stable diagnostic artifact:
- `kvrm-gpu/benchmarks/kvrm_vs_xgrammar_results_mps_quick_xgrammar_diagnostics_seed43_stable.json`
- config:
  - Apple MPS
  - `HuggingFaceTB/SmolLM2-135M-Instruct`
  - quick mode, seed `43`
  - `oracle_warmup=2`
  - `oracle_iterations=4`
  - `--xgrammar-num-shots 4`
  - `--xgrammar-label-permutation`
- test oracle histogram:
  - `TILE_128 x3`
  - `TILE_64 x1`
- zero-shot XGrammar:
  - finite enum emitted `TILE_16 x4`
  - tool-call JSON emitted `TILE_16 x4`
  - semantic correctness stayed `0%`
- permuted-label XGrammar:
  - finite enum emitted `REG_A x4`
  - tool-call JSON emitted `REG_A x4`
  - in that run's mapping, `REG_A -> TILE_16`
  - semantic correctness stayed `0%`
- few-shot XGrammar:
  - finite enum emitted `TILE_64 x4`, giving `25%`
  - tool-call JSON emitted `TILE_128 x3` and `TILE_32 x1`, giving `50%`
- main conclusion:
  - the repeated weak-model `0%` XGrammar result in this repo is mostly semantic label collapse, not a broken parser/grammar path
  - the grammar engine is keeping structure valid, but the model often locks onto one legal surface label and does not recover the latent tile policy zero-shot

Documentation follow-up:
- updated:
  - `kvrm-gpu/README.md`
  - `kvrm-gpu/benchmarks/README.md`
  - `kvrm-gpu/docs/KVRM_XGRAMMAR_BENCHMARK_METHOD.md`
  - `kvrm-gpu/docs/KVRM_VS_XGRAMMAR.md`
- docs now explain:
  - why XGrammar can stay at `0%`
  - how to run the few-shot and permuted-label diagnostics
  - how to interpret the new histogram and mode fields in the artifacts

Additional XGrammar diagnosis after the user asked to continue:
- ran stronger-model stable MPS diagnostic:
  - `kvrm-gpu/benchmarks/kvrm_vs_xgrammar_results_mps_quick_xgrammar_diagnostics_seed43_stable_qwen_0_5b.json`
  - same stable split: oracle histogram `TILE_128 x3`, `TILE_64 x1`
  - zero-shot XGrammar still collapsed to `TILE_16 x4` on both tasks
  - few-shot XGrammar:
    - finite enum: `0.0%`, decoded histogram `TILE_16 x3`, `TILE_32 x1`
    - tool-call JSON: `75.0%`, decoded histogram `TILE_128 x3`, `TILE_32 x1`
  - takeaway: the zero-shot collapse is not limited to the smallest cached model; stronger base-model quality helps the JSON form much more than direct finite-enum selection

- reran the same stable diagnostic on actual CUDA H200 via Vast.ai:
  - synced patched repo to existing instance `32666214`
  - ran:
    - `PYTHONPATH=/workspace/kvrm-gpu ./venv/bin/python benchmarks/kvrm_vs_xgrammar_benchmark.py --quick --seed 43 --device cuda --model HuggingFaceTB/SmolLM2-135M-Instruct --oracle-warmup 2 --oracle-iterations 4 --xgrammar-num-shots 4 --xgrammar-label-permutation --output benchmarks/kvrm_vs_xgrammar_results_cuda_quick_xgrammar_diagnostics_seed43_stable_h200.json`
  - synced artifact back local:
    - `kvrm-gpu/benchmarks/kvrm_vs_xgrammar_results_cuda_quick_xgrammar_diagnostics_seed43_stable_h200.json`
  - zero-shot XGrammar again collapsed to `TILE_16 x4` on both tasks
  - permuted-label control again collapsed to `REG_A x4`, decoding back to `TILE_16`
  - few-shot XGrammar improved to `75.0%` on both finite enum and JSON
  - takeaway: backend is not the root cause of the zero-shot failure, but CUDA seems to help the few-shot constrained-decoding path on this split

- updated docs again:
  - `kvrm-gpu/README.md`
  - `kvrm-gpu/benchmarks/README.md`
  - `kvrm-gpu/EXPERIMENTS.md`
  - `kvrm-gpu/docs/KVRM_XGRAMMAR_BENCHMARK_METHOD.md`
  - `kvrm-gpu/docs/KVRM_VS_XGRAMMAR.md`
- active framing after these runs:
  - XGrammar is best described here as a constrained decoding runtime around a base LM, not a selector architecture
  - KVRM is a purpose-built finite selector model
  - the repeated zero-shot XGrammar failures in this repo are mostly failures of latent policy recovery, not syntax enforcement

Open-world rejection follow-up after the user asked for a stronger proof path:
- added `kvrm-gpu/benchmarks/open_world_rejection_benchmark.py`
- added `kvrm-gpu/tests/test_open_world_rejection_benchmark.py`
- benchmark design:
  - supported workloads come from the same real measured attention-workload space as the current KVRM-vs-XGrammar harness
  - unsupported workloads are still real executable attention configs, but with one axis mutated outside the qualified contract
  - compares:
    - deterministic `contract_guard`
    - `entropy_guard`
    - `nearest_support_distance_guard`
- quick local MPS artifact:
  - `kvrm-gpu/benchmarks/open_world_rejection_results_mps_quick.json`
  - supported test oracle histogram: `TILE_64 x3`, `TILE_128 x5`
  - unsupported OOD axes sampled: `batch_size x3`, `head_dim x1`, `seq_len x4`
  - deterministic contract guard:
    - `0.0%` supported false rejects
    - `100.0%` unsupported rejects
  - entropy guard:
    - unsupported-detection AUROC `0.703`
    - calibrated quick operating point: `12.5%` supported false rejects, `25.0%` unsupported rejects
  - nearest-support-distance guard:
    - unsupported-detection AUROC `0.375`
    - calibrated quick operating point: `25.0%` supported false rejects, `12.5%` unsupported rejects
  - baseline unsupported forced accuracy if auto-dispatched: `62.5%`
- main conclusion:
  - the strongest open-world safety claim is explicit contract rejection, not confidence-only rejection
  - entropy is only a weak supplementary signal in the current evidence
  - nearest feature-space distance is weaker still on this first real MPS artifact
- tests after adding the benchmark: `37 passed`

Documentation follow-up:
- updated:
  - `kvrm-gpu/README.md`
  - `kvrm-gpu/benchmarks/README.md`
  - `kvrm-gpu/EXPERIMENTS.md`
  - `kvrm-gpu/docs/CLAIM_QUALIFICATION_PLAN.md`
  - `kvrm-gpu/docs/ROBUSTNESS_EVIDENCE_MATRIX.md`
- docs now say:
  - deterministic contract checks are the primary open-world safety mechanism
  - uncertainty and nearest-distance guards are supplementary only

Open-world rejection multiseed follow-up after the user said to continue:
- added `kvrm-gpu/benchmarks/open_world_rejection_multiseed.py`
- added `kvrm-gpu/tests/test_open_world_rejection_multiseed.py`
- also tightened `kvrm-gpu/benchmarks/open_world_rejection_benchmark.py` so guard outputs include exact accepted/correct counts for pooled analysis
- local MPS pooled artifact:
  - `kvrm-gpu/benchmarks/open_world_rejection_multiseed_results_mps_quick.json`
  - seeds `42-46`
  - deterministic contract guard:
    - `0.0%` supported false rejects
    - `100.0%` unsupported rejects
  - entropy guard:
    - mean AUROC `0.516`
    - pooled low-FRR operating point: `10.0%` supported false rejects, `10.0%` unsupported rejects
  - nearest-support-distance guard:
    - mean AUROC `0.536`
    - pooled low-FRR operating point: `10.0%` supported false rejects, `15.0%` unsupported rejects
- CUDA H200 pooled artifact via Vast.ai:
  - `kvrm-gpu/benchmarks/open_world_rejection_multiseed_results_cuda_quick_h200.json`
  - same seeds `42-46`
  - deterministic contract guard:
    - `0.0%` supported false rejects
    - `100.0%` unsupported rejects
  - entropy guard:
    - mean AUROC `0.569`
    - pooled low-FRR operating point: `5.0%` supported false rejects, `12.5%` unsupported rejects
  - nearest-support-distance guard:
    - mean AUROC `0.536`
    - pooled low-FRR operating point: `10.0%` supported false rejects, `15.0%` unsupported rejects
- key conclusion after multiseed + cross-backend repetition:
  - contract enforcement is the robust open-world claim
  - entropy and nearest-distance are weak supplementary heuristics, not primary safety mechanisms
- docs updated again to point at the pooled MPS and CUDA artifacts instead of the earlier single-run quick artifact
- tests after this work: `39 passed`

Open-world larger-bank follow-up:
- exposed benchmark overrides in `kvrm-gpu/benchmarks/open_world_rejection_benchmark.py` and `kvrm-gpu/benchmarks/open_world_rejection_multiseed.py` for:
  - support/unsupported workload counts
  - epochs / batch size / hidden dim / learning rate
  - oracle warmup / oracle iterations
  - false-reject target list
- added config-override coverage in `kvrm-gpu/tests/test_open_world_rejection_benchmark.py`
- stronger local artifacts:
  - `kvrm-gpu/benchmarks/open_world_rejection_multiseed_results_mps_nonquick5_largebank.json`
  - `kvrm-gpu/benchmarks/open_world_rejection_multiseed_results_cpu_nonquick5_largebank.json`
- stronger MPS result, seeds `42-46`, 24/16/16/16 workload split, oracle warmup `2`, iterations `4`:
  - contract guard:
    - `0.0%` supported false rejects
    - `100.0%` unsupported rejects
  - entropy guard:
    - mean AUROC `0.491`
    - low-FRR pooled point: `6.2%` supported false rejects, `7.5%` unsupported rejects
  - nearest-support-distance guard:
    - mean AUROC `0.639`
    - even at `15.0%` supported false rejects it rejected only `27.5%` unsupported workloads
- stronger CPU result on the same config:
  - contract guard:
    - `0.0%` supported false rejects
    - `100.0%` unsupported rejects
  - entropy guard:
    - mean AUROC `0.483`
  - nearest-support-distance guard:
    - mean AUROC `0.639`
    - same `27.5%` unsupported rejection at `15.0%` supported false rejects
- CUDA follow-up status:
  - tried to reuse the earlier Vast.ai H200 instances, but both had exited
  - `vastai create instance` failed because the account had no credit
  - no larger-bank CUDA artifact was produced this turn; earlier quick H200 artifact remains `kvrm-gpu/benchmarks/open_world_rejection_multiseed_results_cuda_quick_h200.json`
- main updated conclusion:
  - contract enforcement is the only strong open-world robustness claim
  - entropy stays weak
  - nearest-distance improves when the support bank is widened, but is still not strong enough to treat as a primary safety mechanism
- docs updated to make the larger-bank MPS and CPU artifacts the active evidence and treat the older H200 quick run as corroboration only
- tests after adding the override coverage: `40 passed`

Boundary-stress follow-up after the user asked to continue:
- added `kvrm-gpu/benchmarks/tile_boundary_stress_benchmark.py`
- added `kvrm-gpu/tests/test_tile_boundary_stress_benchmark.py`
- benchmark design:
  - sample anchor workloads inside the supported contract
  - build one-step neighbors along `batch_size`, `num_heads`, `seq_len`, and `head_dim`
  - profile every anchor and neighbor with the real tiled attention path to get measured oracle tiles
  - train the current small KVRM selector on separate profiled supported workloads
  - compare KVRM vs the cold heuristic on:
    - node semantic accuracy
    - regret
    - boundary-edge change precision / recall
- local MPS artifact:
  - `kvrm-gpu/benchmarks/tile_boundary_stress_results_mps_nonquick.json`
  - anchors `10`, unique workloads `56`, active families `6/10`
  - KVRM:
    - all-node accuracy `85.7%`
    - active-boundary accuracy `76.5%`
    - active-boundary mean regret `0.45%`
    - edge agreement `78.2%`
    - change recall `0.0%`
    - predicted `TILE_128` for `55/56` workloads on this split
  - heuristic:
    - all-node accuracy `1.8%`
    - active-boundary mean regret `56.04%`
- local CPU artifact:
  - `kvrm-gpu/benchmarks/tile_boundary_stress_results_cpu_nonquick.json`
  - anchors `10`, unique workloads `56`, active families `8/10`
  - KVRM:
    - all-node accuracy `73.2%`
    - active-boundary accuracy `65.9%`
    - active-boundary mean regret `16.76%`
    - edge agreement `70.9%`
    - change recall `17.6%`
  - heuristic:
    - all-node accuracy `7.1%`
    - active-boundary mean regret `41.07%`
- key conclusion:
  - KVRM is materially better than the cold heuristic on node accuracy and regret
  - but KVRM still underreacts to real local oracle transitions, especially on MPS
  - strong claim update:
    - “low-regret near measured boundaries” is partly supported
    - “good local boundary sensitivity” is not yet supported
- updated active docs:
  - `kvrm-gpu/README.md`
  - `kvrm-gpu/EXPERIMENTS.md`
  - `kvrm-gpu/benchmarks/README.md`
  - `kvrm-gpu/docs/CLAIM_QUALIFICATION_PLAN.md`
  - `kvrm-gpu/docs/ROBUSTNESS_EVIDENCE_MATRIX.md`
- tests after this work: `43 passed`
