[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "kvrm-compiler"
version = "0.1.0"
description = "KVRM compiler CLI"
readme = "README.md"
requires-python = ">=3.9"

[project.scripts]
kvrmc = "main:main"
